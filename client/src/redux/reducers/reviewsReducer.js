import { reviewsAT } from "../ActionTypes/reviewsAT";

const initialState = {
  reviews: {},
};

export const reviewsReducer = (state = initialState, action) => {

  switch (action.type) {

    

    default:
      return state;
  }
};
