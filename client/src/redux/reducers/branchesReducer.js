// import { branchesAT } from "../ActionTypes/branchesAT";

const initialState = {
  branches: {},
};

export const branchesReducer = (state = initialState, action) => {

  switch (action.type) {

    

    default:
      return state;
  }
};
