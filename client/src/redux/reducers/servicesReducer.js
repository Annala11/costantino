// import { servicesAT } from "../ActionTypes/servicesAT";

const initialState = {
  services: {},
};

export const servicesReducer = (state = initialState, action) => {

  switch (action.type) {

    

    default:
      return state;
  }
};
