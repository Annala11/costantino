import { ordersAT } from "../ActionTypes/ordersAT";

const initialState = {
  orders: {},
};

export const ordersReducer = (state = initialState, action) => {

  switch (action.type) {

    

    default:
      return state;
  }
};
